import { FC } from 'react';
import Navbar from './layout/Navbar';

const Header: FC = () => {
  return (
    <div>
      <header>
        <Navbar />
      </header>
    </div>
  );
};

export default Header;